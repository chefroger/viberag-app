# VibeRAG 产品方案 v0.22

> 智能商业文档分析与销售助手
> 版本：v0.22 | 状态：规划中

---

## 1. 产品定位

**一句话：** 扫描客户文档目录，自动理解产品/报价/成交记录，AI 辅助写开发信、话术支持。

**目标客户画像：**
- B2B 销售团队（贸易、制造业、服务业）
- 需要基于历史数据生成个性化客户沟通内容
- 技术能力弱，16GB Windows 办公机主力使用

**核心使用场景：**
1. 导入历史报价单 → 生成新客户报价参考
2. 导入产品手册 → AI 根据需求生成产品推荐话术
3. 导入成交记录 → 生成成交客户维护信
4. 导入客户沟通记录 → 生成跟进对话建议
5. 记录每个客户的画像、每次接触、每封询盘 → 生成针对性话术

**产品文档 vs 客户数据的分离：**
- **产品文档库**（共享）：产品手册、规格参数、报价单模板、成交案例
- **客户画像**（独立）：每个客户一条档案，记录所有历史接触

---

## 2. 核心功能

### 2.1 文档库管理（目录扫描模式）

**用户操作：**
1. 指定一个本地目录路径（如 `D:\客户资料\`）
2. 系统递归扫描该目录下所有文件（含子目录）
3. 每次"刷新"重新扫描增量文件

**支持格式（按版本分批实现）：**

| 版本 | 格式 | 读取方式 | 提取工具 |
|------|------|---------|---------|
| v0.1 | TXT / MD | 原生读取 | — |
| v0.1 | PDF（文字型） | pdfplumber / PyMuPDF 提取文字 | `pdfplumber` 或 `fitz` |
| v0.1 | DOCX | python-docx 提取段落文字 | `python-docx` |
| v0.2 | XLSX / XLS | openpyxl 提取表格数据 | `openpyxl` |
| v0.2 | PPTX | python-pptx 提取幻灯片文字 | `python-pptx` |
| v0.3 | PDF（图片扫描型） | Tesseract OCR | `pytesseract` |
| v0.3 | PNG / JPG / BMP（图片内文字） | Tesseract OCR | `pytesseract` |
| v0.3 | HTML | BeautifulSoup 提取正文文本 | `beautifulsoup4` |

**文件类型自动判断逻辑：**

```
收到文件
    │
    ├─ 扩展名判断
    │    ├─ .pdf  → 用 PyMuPDF 尝试 page.get_text()
    │    │         ├─ 有文字（长度 > 0）→ 直接使用
    │    │         └─ 空 / 字数极少（< 500字）→ 判定为扫描件 → 走 OCR
    │    │
    │    ├─ .pptx → python-pptx 读 shape.text
    │    ├─ .docx → python-docx 读段落
    │    ├─ .xlsx → openpyxl 读单元格
    │    └─ .png/.jpg/.jpeg → Tesseract OCR
    │
    └─ 入库
```

**PDF 扫描件判断实现：**

```python
import fitz  # PyMuPDF

def is_scanned_pdf(filepath: str) -> bool:
    """判断 PDF 是否为扫描件（无文字层）"""
    doc = fitz.open(filepath)
    for page in doc:
        if page.get_text().strip():
            return False  # 有文字，不是扫描件
        if page.get_images():
            return True   # 无文字但有图片，判定为扫描件
    return True
```

**PPTX 提取实现：**

```python
from pptx import Presentation

def extract_pptx(filepath: str) -> str:
    """提取 PPTX 中所有文字，按幻灯片分组"""
    prs = Presentation(filepath)
    results = []
    for i, slide in enumerate(prs.slides):
        texts = []
        for shape in slide.shapes:
            if hasattr(shape, "text") and shape.text.strip():
                texts.append(shape.text.strip())
        if texts:
            results.append(f"=== Slide {i+1} ===\n" + "\n".join(texts))
    return "\n\n".join(results)
```

**图片 OCR 提取实现（Tesseract）：**

```python
import subprocess

def ocr_image(image_path: str, lang: str = "chi_sim+eng") -> str:
    """调用 tesseract 识别图片文字
    安装：brew install tesseract tesseract-lang/tesseract-lang/chi_sim
    """
    result = subprocess.run(
        ["tesseract", image_path, "stdout", "-l", lang],
        capture_output=True, text=True, timeout=60
    )
    return result.stdout

# 使用
print(ocr_image("Kechen-RA.png"))
```

**依赖安装（VibeRAG 启动时检测，缺失则提示）：**

```bash
# Python 库（pip）
pip install pdfplumber python-docx python-pptx openpyxl pymupdf beautifulsoup4

# Tesseract OCR（Mac）
brew install tesseract tesseract-lang/tesseract-lang/chi_sim

# Tesseract OCR（Linux）
sudo apt-get install tesseract tesseract-ocr tesseract-ocr-chi-sim
```

**扫描策略：**
- 首次全量扫描，生成文件指纹（mtime + size）
- 增量判断采用两层策略：
  1. **第一层（快速）**：mtime + size 未变化 → 跳过
  2. **第二层（精确）**：mtime + size 变化 → 计算 content_hash
     - content_hash 未变化 → 只更新 mtime/size 元信息，跳过解析
     - content_hash 变化 → 删除旧 chunk/embedding，重新解析入库
- 删除检测：扫描后比对数据库，发现文件已删除则在数据库中标记为 `deleted`，保留记录不物理删除（避免误删）
- 忽略隐藏文件、系统文件

**分块策略（v0.1）：**
- TXT / MD：按段落切分，优先保留标题/段落边界
- PDF / DOCX：保留页码、段落信息，按段落聚合后切分
- 单 chunk 建议 500~800 tokens，overlap 100 tokens
- chunk 元数据：file_id、file_name、page_number、chunk_index、content_hash、doc_type
- 表格（v0.1 暂不做结构化）：转为 markdown table 或 key-value 文本

**RAG 回答约束（v0.1 必须实现）：**
- 每次回答必须基于检索到的 chunk
- top_k = 5，similarity_threshold = 0.35（可配置）
- 检索结果相似度低于阈值 → 不进入生成阶段，直接返回："未在当前文档库中找到足够相关的信息。"
- 回答必须附带引用来源文件名
- 不允许使用无来源内容回答事实性问题
- max_context_tokens = 4000（可配置）

**错误分类 + 完整错误码：**
- 文件类：FILE_NOT_FOUND / FILE_ACCESS_DENIED / FILE_TOO_LARGE / FILE_LOCKED
- 解析类：UNSUPPORTED_FORMAT / PDF_ENCRYPTED / DOCX_CORRUPTED / OCR_REQUIRED
- 向量类：OLLAMA_NOT_RUNNING / EMBEDDING_MODEL_NOT_FOUND / EMBEDDING_API_TIMEOUT
- 检索类：VECTOR_DB_INIT_FAILED / VECTOR_DIMENSION_MISMATCH
- 生成类：LLM_NOT_CONFIGURED / LLM_API_TIMEOUT / CONTEXT_TOO_LONG / NO_RELEVANT_CONTEXT

**统一错误响应结构：**
```json
{
  "code": "EMBEDDING_MODEL_NOT_FOUND",
  "message": "Embedding 模型未安装",
  "detail": "请先执行 ollama pull nomic-embed-text",
  "recoverable": true
}
```

### 2.2 文档类型规则判断（v0.1）

**详细实现逻辑见 §5（文档识别流程）。**

**v0.1 采用规则判断（不调用 LLM）：**

| 类型 | 识别依据 |
|------|---------|
| 产品说明 | 含规格、型号、材料、尺寸、技术参数等关键词 |
| 报价单 | 含价格、数量、单价、报价日期、MOQ、payment terms |
| 成交记录 | 含合同、签约、订单、已售、出货等关键词 |
| 客户资料 | 含联系人、公司名、地址、电话、邮箱 |
| 沟通记录 | 含对话、邮件、微信、会议、沟通记录 |
| 样品信息 | 含样品、打样、封样、确认等 |
| 其他 | 未能归类 |

- **识别顺序**：先文件名（快速过滤）→ 再内容关键词（精确匹配）
- **优先级**：报价单 > 成交记录 > 产品说明 > 客户资料 > 沟通记录 > 样品信息 > 其他
- **v0.1 分类不计费、不上传内容到云端**

**LLM 分类（v0.3+ 云端增强模式，可选）：**
- 用户开启"云端增强模式"后可用
- 将文件片段 + 文件名发给 LLM 判断类型
- 少量文件用 few-shot prompt
- 切换前明确提示：文件片段会上传到第三方 LLM 服务商

**📝 参考借鉴 — PageIndex 推理式分类思路（未来优化方向）：**

> 参考项目：[VectifyAI/PageIndex](https://github.com/VectifyAI/PageIndex)（⭐23.7k）— 无向量、基于 LLM 推理的 RAG 系统。

传统向量检索依赖"语义相似度"，但 similarity ≠ relevance。PageIndex 的核心思路是**用 LLM 在索引结构上做推理判断**，比向量匹配更准。

对 VibeRAG 的启发：

| 启发点 | 当前方案 | 可升级方向 |
|--------|---------|-----------|
| 文档分类 | §2.2 靠规则/关键词 | 未来可用 LLM 一次调用判断类型（调用量小，不影响隐私） |
| 检索重排 | 纯向量相似度 | 未来可两阶段：向量召回 Top-K → LLM 过滤重排（判断"相关"而非"像"）|
| 结构化文档 | 暴力 chunk | XLSX/PPTX 可借鉴"自然章节"思路，按 Sheet/幻灯片结构解析 |

**注意：** PageIndex 全程需要云端 LLM（建索引极贵，几十到上百次调用），与 VibeRAG 的隐私设计冲突。v0.1 保持纯向量检索，以上为 v1.0+ 的参考方向。

### 2.3 向量存储

**Embedding 引擎：** Ollama + `nomic-embed-text`

- 模型：nomic-embed-text（~274MB，Ollama 自动下载）
- 启动：系统启动时自动检测 Ollama 是否运行，未运行则提示用户启动
- 向量维度：768
- 存储：SQLite 内（通过 sqlite-vec 扩展）

**向量表结构：**
```sql
CREATE TABLE chunk_embedding (
    id INTEGER PRIMARY KEY,
    chunk_id INTEGER,
    file_id INTEGER,
    doc_type TEXT,  -- 识别出的文档类型
    embedding_id INTEGER  -- sqlite-vec 的 embedding ID
);
```

### 2.4 LLM 生成（API Key）

**支持模型（客户自填 API Key）：**
- OpenAI GPT-4o / GPT-4o-mini
- Claude 3.5 Sonnet / 3.7 Sonnet
- 硅基流动（SiliconFlow）
- MiniMax
- 其他 OpenAI-compatible API

**配置界面：** 设置页面填写 API Key 和模型选择

### 2.5 Agent 角色定义（两层 System Prompt）

**Layer 1 — 通用基础角色（全局设置）：**
```
你是一名资深外贸 B2B 销售专家，拥有 X 年出口经验，精通以下能力：
- 根据客户需求推荐合适的产品型号和配置
- 撰写专业、个性化、有说服力的英文开发信和跟进邮件
- 提供电话/视频销售话术，处理客户异议，促成成交
- 基于历史报价和成交记录给出合理报价建议
- 理解产品技术规格，能与客户工程师直接沟通
- 注意不同国家/地区的商务礼仪和沟通习惯

语气风格选项：正式 / 专业友善 / 简洁直接
回复格式：依据场景自动适配（邮件 / 话术 / 报价单格式）
```

**Layer 2 — 产品线专属角色（每个文档库独立配置）：**
```
【产品线名称】：XXX
【目标市场】：东南亚 / 中东 / 欧美（影响报价策略和交期表述）
【主打产品】：型号A、型号B、型号C
【客户关注点】：价格敏感 / 质量优先 / 交期紧 / 认证要求（CE/UL/ISO）
【常见问题】：交货期MOQ、 payment terms、定制能力
【沟通禁忌】：某些表达方式在目标市场可能不合适
【附加服务】：OEM/ODM能力、logo定制、包装要求
```

**实际调用时的完整 prompt = Layer 1 + Layer 2 + 客户画像 + 检索到的上下文 + 用户输入**

**配置界面：** 全局基础角色设置一次，每个文档库有独立的产品线扩展编辑框。

### 2.6 智能生成功能

#### 客户开发信
- 用户输入：客户公司名/需求描述
- 系统：从向量库检索相关产品说明 + 历史报价单 + 成交案例
- 生成：根据模板填充个性化开发信（语气可调：正式/友好/专业）

#### 销售话术支持
- 用户输入：客户类型/当前沟通阶段（首次接触/询价/议价/成交）
- 系统：检索相关产品信息 + 历史沟通记录 + 成交案例
- 生成：分场景话术（开场白/产品介绍/异议处理/促成）

#### 报价建议
- 用户输入：客户需求描述/产品型号
- 系统：检索历史报价单中相似配置的价格
- 生成：参考报价区间 + 调整建议

#### 对话建议
- 用户输入：客户名称
- 系统：检索该客户历史资料 + 沟通记录
- 生成：建议跟进话题 + 注意事项

### 2.7 客户阶段体系（四级）

**背景：** 不是所有潜在客户都需要立即建完整档案。VibeRAG 按销售阶段区分处理方式，阶段越早越轻量，进入正式销售流程才触发客户建档。

**「默认对话」例外：** 侧边栏永久保留一个「默认对话」入口，独立于客户体系之外，不关联任何客户、Prospect 或文档库。用户没有任何客户资料时也可以直接使用。

#### 四级阶段定义

| 阶段 | 数据库归属 | Sidebar 展示 | 说明 |
|------|-----------|--------------|------|
| 询盘（inquiry） | `prospect` 表，状态=询盘 | 📨 灰色行 | 一次性问价/样品，未明确跟进 |
| 新开发（newdev） | `prospect` 表，状态=新开发 | 📋 灰色行 | 开始主动联系，有往来积累 |
| 意向客户（intention） | `customer` 表（去重建档后） | 👤 正式客户 | 有明确需求，进入报价/谈判 |
| 成交客户（customer） | `customer` 表，状态=成交 | 👤 正式客户 | 首次成交，档案永久保留 |

#### 升级触发机制

| 当前阶段 | 升级动作 | 触发方式 | 操作 |
|---------|---------|---------|------|
| 询盘 | → 新开发 | 用户点「开始跟进」或 AI 建议升级 | 创建 `prospect` 记录 |
| 新开发 | → 意向客户 | 用户点「确认添加为客户」 | 触发 §6.4 去重流程 → 创建 `customer` |
| 意向客户 | → 成交客户 | 用户标记「已成交」 | `customer.status` 改为成交 |

#### 降级处理

- 意向客户 → 新开发：长期无跟进，用户可手动降级
- 任何阶段 → 询盘：彻底失去联系，降级记录最后接触时间

#### Prospect 表结构

```
prospect
├── id              UUID（主键）
├── name            潜在客户名称（如"伊朗石油·张工"）
├── source          来源描述（如"广交会名片"、"询盘邮件"）
├── stage           enum: inquiry | newdev | intention
│                    → inquiry:  询盘（最早期，一次性问价）
│                    → newdev:   新开发（开始主动联系）
│                    → intention:意向客户（触发去重建档前）
├── notes           自由文本备注
├── created_at      首次记录时间
├── updated_at      最后更新时间
└── converted       bool：是否已转化为 customer（转化后保留记录）
```

**stage 流转：** `inquiry` → `newdev` → `intention` →（触发去重）→ 创建 `customer`

#### 与 customer 表的关系

- **未转化前**：`prospect` 独立存在，无 `customer_id`
- **转化时**：触发 §6.4 去重流程，确认不重复后创建 `customer`，`prospect.converted=TRUE`
- **已成交客户**：直接在 `customer` 表内流转，不再经过 `prospect`

---

### 2.8 强制记忆机制（防遗忘设计）

VibeRAG 解决的核心问题：**不依赖 AI "记起来"，而是用系统强制保证记忆在场。**

#### 根因分析：gbrain/Agent 为什么会忘

| 失效模式 | 原因 |
|---------|------|
| 上下文割裂 | 新会话开始时没有任何自动加载，历史全靠猜 |
| 依赖手动触发 | 必须"主动想起来去查"，而不是"系统自动注入" |
| 入口触发点缺失 | 没有机制在"生成回复前"强制验证记忆在场 |

**结论：工具存在 ≠ 记忆可用。需要"强制入口"机制。**

#### 核心原则：入口即加载

**每次生成 AI 回复前，必须自动完成以下步骤（对用户不可见）：**

```
用户发来消息
    ↓
识别客户身份（发件人 / 客户名 / 客户ID）
    ↓
┌──────────────────────────────────────────┐
│  【强制加载层】无用户提示，系统自动执行     │
│                                          │
│  Layer 3 → 加载客户画像（来源、偏好、预算、竞争态势）│
│  Layer 1 → 加载最近3次会话摘要 + 未完成任务│
│  Layer 2 → 检索相关产品文档片段            │
│                                          │
│  → 合并为完整上下文                       │
│  → 注入 System Prompt                    │
└──────────────────────────────────────────┘
    ↓
AI 基于完整上下文生成回复
    ↓
对话结束 → 自动写回 Layer 1（会话摘要）+ Layer 3（画像更新）
```

#### 实现要求

**1. 入口拦截器（Inbound Interceptor）**
- 位置：FastAPI 中间件或路由层
- 触发时机：每个 `/api/ask/*` 请求前
- 动作：
  1. 从请求中提取客户身份（客户ID 或 公司名）
  2. 查询 Layer3 客户画像
  3. 查询 Layer1 最近会话摘要
  4. 附加到请求上下文，注入 System Prompt
- **强制执行**：以上步骤有任何一步失败，返回错误而不是继续生成

**2. 上下文完整性校验**
- 生成回复前，校验以下字段是否存在：
  - `customer_id`（必须，但默认对话除外）
  - `last_conversation_summary`（最近一次，无则警告）
  - `pending_tasks`（本客户的未完成任务）
- **默认对话例外**：无 customer_id，跳过客户画像加载，直接进入生成流程，不触发校验拦截
- 缺失关键字段 → 提示用户"请完善客户信息"并阻止生成，直到用户补充

**3. 自动写回（Auto Write-back）**
- 对话结束触发（用户关闭会话 / 5分钟无操作 / 显式点击"完成"）
- 自动执行：
  - 生成本次会话摘要（LLM 提取关键信息）
  - 更新 `conversation_turn` 表
  - 更新 `customer` 表的最后接触时间 + 画像字段

**4. 待确认闭环追踪**
- 每次生成内容中的"待确认项"（`pending_confirm` 字段）
- 会话结束时展示未完成确认项
- 下次打开该客户时，自动显示并提示完成

#### 与 Hermes/gbrain 的本质区别

| | Hermes/gbrain | VibeRAG |
|--|--------------|---------|
| 触发方式 | 手动调用（我可能忘） | 系统强制拦截（无法绕过） |
| 验证机制 | 无 | 缺失字段直接阻止生成 |
| 写回时机 | 依赖自觉 | 强制自动 |
| 遗忘后果 | 对话质量下降但无法感知 | 关键字段缺失时阻止生成 |

---

### 2.9 客户画像与 CRM（销售情报）

每个客户独立建档，随时间积累完整画像。

**客户画像字段：**
| 字段 | 说明 |
|------|------|
| 公司名称 | 必填 |
| 联系人 | 姓名、职位、电话、邮箱 |
| 来源渠道 | 展会/B2B平台/转介绍/主动开发 |
| 客户类型 | 进口商/批发商/经销商/终端用户 |
| 意向产品 | 本次询价的产品线 |
| 预算区间 | 客户透露的预算范围 |
| 决策周期 | 客户透露的时间节点 |
| 竞争态势 | 客户同时在对比哪些供应商 |
| 偏好与禁忌 | 沟通风格偏好、特殊要求 |
| 备注 | 销售员手动记录 |

**每次接触自动记录：**
| 接触类型 | 记录内容 |
|----------|---------|
| 询盘 Inquery | 来源、询价产品、数量、问的原话 |
| 报价 Quotation | 报价产品、价格、条件、有效期 |
| 样品 Sample | 样品名称、数量、寄样时间、物流单号 |
| 会话 Chat | 微信/邮件/会面的关键内容摘要 |
| 订单 Order | 成交产品、数量、金额、交货期 |
| 售后 After-sales | 问题描述、处理结果 |

**AI 辅助积累画像（v0.2 起）：**
- 每次生成内容（开发信/话术/报价）后，AI 提取关键信息生成「画像更新建议」
- **不自动写入**，以弹窗/面板展示更新建议内容
- 用户逐条确认：接受的字段才写入档案，拒绝的字段忽略
- 支持手动补充/修正任意字段
- **禁止将 AI 推断内容直接写入**：`preferences`、`competitors`、`budget` 等高风险字段必须用户确认

---

## 3. 系统架构

### 3.1 架构图

```
┌──────────────────────────────────────────────────────┐
│                    客户端（浏览器）                    │
│              HTML + htmx + Pico CSS                   │
└──────────────────────┬───────────────────────────────┘
                       │ HTTP
┌──────────────────────▼───────────────────────────────┐
│                 FastAPI 后端                      │
│                                                        │
│  ┌──────────────────────────────────────────────────┐ │
│  │ 【入口拦截器】每个请求强制执行，无法绕过             │ │
│  │ 1. 提取客户身份                                   │ │
│  │ 2. 加载 Layer3 客户画像                           │ │
│  │ 3. 加载 Layer1 最近会话摘要                       │ │
│  │ 4. 注入 System Prompt                            │ │
│  │ 5. 校验字段完整性 → 缺失则阻止生成                │ │
│  └──────────────────────────────────────────────────┘ │
│                                                        │
│  路由：                                                │
│  /api/library/scan      — 扫描文档目录                  │
│  /api/library/status    — 扫描进度                      │
│  /api/library/files     — 文件列表与分类                │
│  /api/customer/list     — 客户列表                      │
│  /api/customer/<id>     — 客户画像（GET/PATCH）         │
│  /api/session/start     — 开启会话（强制加载三层上下文） │
│  /api/session/<id>/turn — 发送消息（每轮记录到 turn 表）│
│  /api/session/<id>/end  — 结束会话（自动写回摘要+画像）│
│  /api/session/<id>/clear— 清空对话历史（仅默认对话）  │
│  /api/ask/letter        — 生成开发信                    │
│  /api/ask/script        — 生成话术                      │
│  /api/ask/quote         — 报价建议                      │
│  /api/ask/followup      — 对话建议                      │
│  /api/settings          — LLM 配置                      │
└────┬─────────────────┬───────────────┬────────────────┘
     │                 │               │
     ▼                 ▼               ▼
┌──────────┐    ┌──────────┐    ┌────────────────┐
│  SQLite  │    │sqlite-vec│    │   Ollama       │
│  (元数据) │    │ (向量)   │    │ nomic-embed   │
│          │    │          │    │   (本地)       │
│  客户画像 │    │          │    └────────────────┘
│  会话摘要 │    └──────────┘             │
│  对话轮次 │                              │
└──────────┘                              │
     │                                    │
     │   入口拦截器强制加载                │
     │◄───────────────────────────────────┘
     │   客户画像 + 会话摘要 + RAG检索片段
     │
     ▼
┌──────────────────────────────────────────────────────┐
│              LLM API（客户自备 Key）                  │
│        OpenAI / Claude / 硅基流动 / MiniMax           │
└──────────────────────────────────────────────────────┘
```

### 3.1.1 入口拦截器实现

```python
# middleware/inbound_interceptor.py
from fastapi import Request, HTTPException
from sqlalchemy.orm import Session

async def inbound_interceptor(request: Request, db: Session):
    """每个 /api/ask/* 请求的强制前置处理器"""

    # 1. 提取会话类型和客户身份
    session_type = request.headers.get("X-Session-Type")  # 'default' | 'customer'
    customer_id = request.headers.get("X-Customer-ID")
    if not customer_id:
        customer_id = request.query_params.get("customer_id")

    # 2. 默认对话：跳过客户层，直接放行
    if session_type == "default" or not customer_id:
        request.state.viberag_context = {
            "mode": "default",
            "customer": None,
            "recent_summary": None,
            "missing_fields_warning": []
        }
        return request.state.viberag_context

    # 3. 正式客户：加载 Layer3 客户画像
    customer = db.query(Customer).filter(Customer.id == customer_id).first()
    if not customer:
        raise HTTPException(404, "客户不存在")

    # 4. 加载 Layer1 最近会话摘要
    recent = db.query(SessionSummary)\
        .filter(SessionSummary.customer_id == customer_id)\
        .order_by(SessionSummary.updated_at.desc())\
        .first()

    # 5. 校验关键字段（正式客户才校验）
    missing = []
    if not customer.budget and not customer.notes:
        missing.append("客户偏好（建议完善：预算/偏好）")
    if not recent:
        missing.append("历史会话摘要（新客户或久未跟进）")

    # 6. 注入上下文
    context = {
        "mode": "customer",
        "customer": customer_to_dict(customer),
        "recent_summary": session_summary_to_dict(recent) if recent else None,
        "missing_fields_warning": missing  # 传给前端显示，但不阻止
    }

    request.state.viberag_context = context
    return context
```

**三种模式的行为：**

| 模式 | customer_id | Layer3 加载 | Layer1 加载 | 校验拦截 |
|------|-----------|------------|------------|---------|
| 默认对话 | 无 | ❌ 跳过 | ❌ 跳过 | ❌ 跳过 |
| 正式客户 | 有 | ✅ 强制 | ✅ 强制 | ✅ 警告但不阻止 |

### 3.2 技术栈

| 组件 | 技术 | 说明 |
|------|------|------|
| 后端框架 | FastAPI | 异步并发扫描 |
| 数据库 | SQLite | 元数据存储 |
| 向量 | sqlite-vec | 轻量向量索引 |
| Embedding | Ollama + nomic-embed-text | 本地运行 |
| PDF 文字 | pdfplumber | 可复制文字 PDF |
| PDF OCR | Tesseract (pytesseract) | 图片型 PDF 先尝试文字提取，失败则 OCR |
| 图片 OCR | Tesseract (pytesseract) | PNG/JPG/BMP 内嵌文字提取 |
| OCR 训练数据 | tessdata (chi_sim+eng) | 打包进分发包，跨平台无需用户安装 |
| Office | python-docx / openpyxl / python-pptx | 文档解析 |
| LLM 路由 | 直接调用（openai / anthropic 库） | 不用 LiteLLM，简单直接 |
| 前端 | htmx + 纯 HTML + Pico CSS | 无需打包 |
| 桌面打包 | PyInstaller | Windows (.exe) + macOS (.app) 双平台 |
| **跨平台** | Python 3.11+，所有依赖跨平台 | Windows 10+ / macOS 12+ |

### 3.3 RAM 占用估算

| 组件 | 预估 RAM |
|------|---------|
| FastAPI + Python | 150-200 MB |
| SQLite + sqlite-vec | 100-150 MB |
| Ollama (nomic-embed-text) | 300-500 MB |
| Tesseract OCR | 100-200 MB |
| 文档解析库（按需加载） | 50-100 MB |
| **总计** | **700-1150 MB** |

有充足余量给办公软件。

#### 3.3.1 OCR 跨平台部署

**Tesseract** 是唯一选择——跨 Windows / macOS Intel / macOS Apple Silicon / Linux，MIT 协议商用无忧。

**为什么不用 RapidOCR / EasyOCR：**
- RapidOCR 的 onnxruntime 在 macOS Intel 上兼容性问题多
- EasyOCR 部署包 ~2GB，太重
- Tesseract 成熟稳定，打包简单

**OCR 处理策略（分层降级）：**
```
图片型 PDF / 扫描件
  │
  ├─ 1. PyMuPDF (fitz) 尝试提取文字层
  │      ├─ page.get_text() 有内容（> 500字）→ 直接用文字，跳过 OCR（快）
  │      └─ page.get_text() 为空或极少
  │              ├─ page.get_images() 有图片 → 判定为扫描件
  │              └─ 无图片 → 判定为空白页/损坏
  │
  └─ 2. 判定为扫描件 → 调用 Tesseract OCR
         ├─ 图片转灰度 + 二值化预处理（提升识别率）
         ├─ 语言包：chi_sim（中文）+ eng（英文）同时加载
         │      Mac:   brew install tesseract tesseract-lang/tesseract-lang/chi_sim
         │      Linux: sudo apt-get install tesseract tesseract-ocr-chi-sim
         └─ 输出纯文本，按行/段落切分
```

**扫描型 PDF 预检机制（与 §2.1 联动）：**
- 先用 PyMuPDF 提取文字
- 字数 < 500 字 → 判定为扫描型 → 走 Tesseract OCR
- 原因：很多 PDF 是"文字型但编码混乱"，不是纯扫描，直接 OCR 浪费性能

**Tesseract 语言包打包方式：**
```
VibeRAG/
└── tessdata/
    ├── chi_sim.traineddata   (~20 MB)
    └── eng.traineddata       (~12 MB)
```
- 打包进 PyInstaller / Tauri 资源目录
- 运行时 `pytesseract.pytesseract.tesseract_cmd` 指向 bundled 路径
- **用户零配置**，开箱即用，不需要单独安装 Tesseract

**OCR 性能参考：**
- 文字型 PDF：< 1s/页（PyMuPDF 直接读文字）
- 扫描型 PDF：5-15s/页（OCR，视图片复杂度）
- 图片 PNG/JPG：2-5s/张（OCR）
- 批量扫描文档时显示进度条，不卡 UI

### 3.4 数据模型

```sql
-- 项目/文档库（每个用户目录 = 一个库）
CREATE TABLE library (
    id INTEGER PRIMARY KEY,
    name TEXT NOT NULL,       -- 库名称（用户取名）
    root_path TEXT NOT NULL,   -- 文档根目录
    created_at TIMESTAMP,
    last_scan_at TIMESTAMP
);

-- 扫描到的文件
CREATE TABLE file (
    id INTEGER PRIMARY KEY,
    library_id INTEGER REFERENCES library(id) ON DELETE CASCADE,
    file_path TEXT NOT NULL,   -- 绝对路径
    file_name TEXT NOT NULL,
    file_ext TEXT,
    file_size INTEGER,
    file_mtime REAL,           -- 修改时间戳（用于增量扫描）
    content_hash TEXT,          -- SHA256 内容哈希（用于精确增量判断）
    doc_type TEXT,              -- 识别出的文档类型
    doc_type_confidence REAL,   -- 分类置信度
    status TEXT DEFAULT 'pending',  -- pending / processing / indexed / failed / deleted / unsupported_ocr_required
    error_message TEXT,         -- 失败原因
    chunk_count INTEGER DEFAULT 0,   -- 分块数量
    is_processed BOOLEAN DEFAULT 0,
    deleted BOOLEAN DEFAULT 0,   -- 软删除标记
    created_at TIMESTAMP,
    updated_at TIMESTAMP,
    FOREIGN KEY (library_id) REFERENCES library(id) ON DELETE CASCADE
);

-- 文本块
CREATE TABLE chunk (
    id INTEGER PRIMARY KEY,
    file_id INTEGER REFERENCES file(id) ON DELETE CASCADE,
    library_id INTEGER REFERENCES library(id) ON DELETE CASCADE,
    content TEXT NOT NULL,
    token_count INTEGER,         -- token 数量
    chunk_index INTEGER,
    page_number INTEGER,         -- 所属页码
    file_name TEXT,              -- 冗余存储，方便检索展示
    doc_type TEXT,                -- 所属文档类型
    content_hash TEXT,            -- 本块内容的哈希
    created_at TIMESTAMP,
    FOREIGN KEY (file_id) REFERENCES file(id) ON DELETE CASCADE,
    FOREIGN KEY (library_id) REFERENCES library(id) ON DELETE CASCADE
);

-- 向量（sqlite-vec）
CREATE TABLE chunk_vector (
    id INTEGER PRIMARY KEY,
    chunk_id INTEGER REFERENCES chunk(id) ON DELETE CASCADE,
    model TEXT DEFAULT 'nomic-embed-text',   -- embedding 模型名
    dimension INTEGER DEFAULT 768,            -- 向量维度
    embedding_id INTEGER,        -- sqlite-vec 内部 ID
    FOREIGN KEY (chunk_id) REFERENCES chunk(id) ON DELETE CASCADE
);

-- 会话（每个独立对话线程）
CREATE TABLE session (
    id TEXT PRIMARY KEY,       -- UUID
    customer_id INTEGER REFERENCES customer(id),
    library_id INTEGER REFERENCES library(id),
    session_type TEXT,  -- 'letter' | 'script' | 'quote' | 'followup'
    status TEXT DEFAULT 'active',  -- 'active' | 'ended'
    input_text TEXT,
    output_text TEXT,
    created_at TIMESTAMP,
    ended_at TIMESTAMP      -- 新增：会话结束时间
);

-- 客户档案
CREATE TABLE customer (
    id INTEGER PRIMARY KEY,
    company_name TEXT NOT NULL,
    contact_name TEXT,
    contact_title TEXT,
    phone TEXT,
    email TEXT,
    source TEXT,           -- 展会/B2B平台/转介绍/主动开发
    customer_type TEXT,    -- 进口商/批发商/经销商/终端用户
    budget TEXT,           -- 预算区间
    decision_cycle TEXT,   -- 决策周期
    competitors TEXT,      -- 在对比的供应商
    preferences TEXT,      -- 偏好与禁忌
    notes TEXT,
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);

-- 接触记录（每次与客户的互动）
CREATE TABLE interaction (
    id INTEGER PRIMARY KEY,
    customer_id INTEGER REFERENCES customer(id) ON DELETE CASCADE,
    interaction_type TEXT NOT NULL,  -- 'inquiry' | 'quotation' | 'sample' | 'chat' | 'order' | 'aftersales'
    summary TEXT NOT NULL,             -- AI 生成的摘要
    raw_content TEXT,                 -- 原始内容（询盘原文/聊天记录等）
    products TEXT,                    -- 涉及的产品（逗号分隔）
    amount TEXT,                      -- 金额/数量
    created_at TIMESTAMP
);

-- 客户专属文档关联（某客户指定了哪些文档/产品线）
CREATE TABLE customer_library (
    id INTEGER PRIMARY KEY,
    customer_id INTEGER REFERENCES customer(id) ON DELETE CASCADE,
    library_id INTEGER REFERENCES library(id) ON DELETE CASCADE,
    note TEXT                         -- 比如"主打型号A和B"
);

-- 会话摘要（解决 Agent 金鱼记忆）
CREATE TABLE session_summary (
    id INTEGER PRIMARY KEY,
    session_id TEXT NOT NULL,       -- 会话唯一标识（UUID）
    customer_id INTEGER REFERENCES customer(id),
    summary_text TEXT NOT NULL,       -- AI 生成的会话摘要
    key_decisions TEXT,              -- 关键结论：已确认事项
    pending_items TEXT,               -- 待确认事项
    mentioned_products TEXT,          -- 本次提到过的产品
    mentioned_prices TEXT,            -- 提到过的价格/报价
    turn_count INTEGER DEFAULT 0,     -- 对话轮数累计
    updated_at TIMESTAMP
);

-- 对话记录（每轮消息带时间戳，维持时序）
CREATE TABLE conversation_turn (
    id INTEGER PRIMARY KEY,
    session_id TEXT NOT NULL,       -- 关联的会话ID
    customer_id INTEGER REFERENCES customer(id),
    turn_index INTEGER NOT NULL,     -- 第几轮对话（递增）
    role TEXT NOT NULL,              -- 'user' | 'assistant'
    content TEXT NOT NULL,           -- 原始消息内容
    created_at TIMESTAMP NOT NULL,   -- 精确到秒的时间戳 ← 核心
    products_mentioned TEXT,          -- 本轮提到哪些产品
    prices_mentioned TEXT,           -- 本轮提到哪些价格
    intent TEXT,                     -- 本轮用户意图（由AI提取）
    pending_from_this_turn TEXT,     -- 本轮产生的待确认事项
    FOREIGN KEY (customer_id) REFERENCES customer(id) ON DELETE CASCADE
);
```

---

## 4. 三层记忆系统

LLM 的上下文有限，每次对话都会遗忘早期信息。VibeRAG 用三层记忆对抗这个问题。

```
┌─────────────────────────────────────────────┐
│  LAYER 1: Session Memory（会话记忆）            │
│  当前聊天窗口内的关键信息，永久摘要化存储         │
│  • 客户已确认的需求/偏好                        │
│  • 已推荐/讨论过的产品型号                       │
│  • 已报价的价格和客户反馈                        │
│  • 待确认事项（pending items）                  │
│  → conversation_turn 表：每轮带精确时间戳        │
│  → session_summary 表：AI 生成的会话摘要        │
└─────────────────────────────────────────────┘
                    +
┌─────────────────────────────────────────────┐
│  LAYER 2: Product Knowledge（RAG 检索）        │
│  产品文档库，向量化存储，按需检索                 │
│  • 产品规格、手册、参数                          │
│  • 历史报价单、成交记录                          │
│  • FAQ、常见问题                               │
│  → 存储在 chunk + chunk_vector 表              │
└─────────────────────────────────────────────┘
                    +
┌─────────────────────────────────────────────┐
│  LAYER 3: Customer Profile（客户画像）          │
│  独立建档，长期积累                             │
│  • 联系人、渠道、类型、偏好禁忌                   │
│  • 历史接触记录（询盘/报价/样品/订单/售后）        │
│  • 客户专属的文档库关联                          │
│  → 存储在 customer + interaction 表            │
└─────────────────────────────────────────────┘
                    ↓
          合并注入 LLM Context
```

**会话摘要生成时机：**
- 每轮对话结束后，LLM 自动生成/更新摘要
- 当上下文 token 超过阈值（如 4000 tokens），强制生成新摘要并清空会话历史
- 用户点击"继续"时，把最新摘要作为起点

**每轮对话的结构（存入 conversation_turn 表）：**
```
turn_index: 3
role: user
content: "你们的X300价格能降到多少？"
created_at: 2026-04-27 09:15:32
products_mentioned: "X300"
prices_mentioned: null
intent: "询问X300价格"
pending_from_this_turn: "X300报价"
```

**会话摘要格式（含时序）：**
```
【会话摘要 - 客户A · 2026-04-27 09:00-09:20】
时间线：
- 09:02 客户首次询问工业阀门，主打型号 X200、X300
- 09:08 发送 X200 报价：FOB $45/pc，MOQ 100pcs
- 09:15 客户反馈价格偏高，说在对比 B 供应商
- 09:18 客户询问 X300 价格 ← 待回复

已确认：
- 目标市场：东南亚，认证要求 CE
- X200 报价已发送：FOB $45/pc，MOQ 100pcs

待确认：
- [ ] X300 的价格（09:15 客户主动询问）
- [ ] 交期（客户问 3 周能否完成）
- [ ] 样品政策（是否免费样品）
```

**实际 LLM 输入 = Layer1(摘要) + Layer2(RAG) + Layer3(画像) + SystemPrompt + 当前输入**

---

## 5. 文档识别流程

**支持范围（按版本）：**
- v0.1：TXT / MD / 文字型 PDF / DOCX
- v0.2：XLSX / XLS / PPTX
- v0.3：扫描型 PDF（OCR）/ 图片内文字 OCR / HTML

**v0.1 流程（不调用 LLM）：**
```
用户指定目录
       │
       ▼
  递归文件扫描
  (仅收集 TXT / MD / PDF / DOCX)
       │
       ▼
  增量判断：mtime + size + content_hash
  (见 §2.1 增量更新策略)
       │
   无变化 → 跳过
   新文件 → 进入处理
   有变化 → 重新解析
       │
       ▼
  文件类型自动判断
  ┌─────────────────────────────────────┐
  │  .pdf  → PyMuPDF page.get_text()    │
  │          ├─ 有文字（>500字）→ 直接用 │
  │          └─ 无/极少 → 走 OCR         │
  │  .pptx → python-pptx shape.text     │
  │  .docx → python-docx 读段落          │
  │  .txt  → 原生读                      │
  └─────────────────────────────────────┘
       │
       ▼
  内容分块
  (按段落，500-800 tokens，overlap 100)
       │
       ▼
  文档类型规则判断
  (扩展名 + 文件名/内容关键词，不调 LLM)
       │
       ▼
  Ollama nomic-embed-text 向量化
       │
       ▼
  存入 sqlite-vec
```

**核心提取函数实现：**

```python
# PDF 文字提取
import fitz

def extract_pdf_text(filepath: str) -> str:
    """提取 PDF 中所有文字（文字型 PDF）"""
    doc = fitz.open(filepath)
    results = []
    for i, page in enumerate(doc):
        text = page.get_text()
        if text.strip():
            results.append(f"--- 第{i+1}页 ---\n{text}")
    return "\n".join(results)

# PDF 扫描件判断
def is_scanned_pdf(filepath: str) -> bool:
    """判断 PDF 是否为扫描件（无文字层）"""
    doc = fitz.open(filepath)
    for page in doc:
        if page.get_text().strip():
            return False
        if page.get_images():
            return True
    return True

# PPTX 提取
from pptx import Presentation

def extract_pptx(filepath: str) -> str:
    """提取 PPTX 中所有文字"""
    prs = Presentation(filepath)
    results = []
    for i, slide in enumerate(prs.slides):
        texts = []
        for shape in slide.shapes:
            if hasattr(shape, "text") and shape.text.strip():
                texts.append(shape.text.strip())
        if texts:
            results.append(f"=== Slide {i+1} ===\n" + "\n".join(texts))
    return "\n\n".join(results)

# 图片 OCR
import subprocess

def ocr_image(image_path: str, lang: str = "chi_sim+eng") -> str:
    """调用 tesseract 识别图片文字"""
    result = subprocess.run(
        ["tesseract", image_path, "stdout", "-l", lang],
        capture_output=True, text=True, timeout=60
    )
    return result.stdout
```

**图片文件的处理（v0.1）：**
- PNG / JPG / BMP 内嵌文字 → Tesseract OCR 提取
- 返回状态 `indexed`（成功）或 `failed`（OCR 失败），不报致命错误

**分块策略（v0.1）：**
- TXT / MD：按段落切分，优先保留标题/段落边界
- PDF / DOCX：保留页码、段落信息，按段落聚合后切分
- 单 chunk 建议 500~800 tokens，overlap 100 tokens
- chunk 元数据：file_id、file_name、page_number、chunk_index、content_hash、doc_type
- 表格（v0.1 暂不做结构化）：转为 markdown table 或 key-value 文本

**v0.1 文档类型规则判断（不调用 LLM）：**

| 类型 | 识别依据 |
|------|---------|
| 产品说明 | 含规格、型号、材料、尺寸、技术参数等关键词 |
| 报价单 | 含价格、数量、单价、报价日期、MOQ、payment terms |
| 成交记录 | 含合同、签约、订单、已售、出货等关键词 |
| 客户资料 | 含联系人、公司名、地址、电话、邮箱 |
| 沟通记录 | 含对话、邮件、微信、会议、沟通记录 |
| 样品信息 | 含样品、打样、封样、确认等 |
| 其他 | 未能归类 |

- **识别顺序**：先文件名（快速过滤）→ 再内容关键词（精确匹配）
- **优先级**：报价单 > 成交记录 > 产品说明 > 客户资料 > 沟通记录 > 样品信息 > 其他
- **v0.1 分类不计费、不上传内容到云端**

---

## 6. 界面原型（文字描述）

### 6.1 侧边栏（对话 + 客户合一）

**初始状态（无任何文档库）：** 侧边栏只有一个入口，不可删除，不可重命名，始终置顶。

```
┌─────────────────────────┐
│ 💬 默认对话             │ ← 始终存在，无文档库时唯一入口
├─────────────────────────┤
│  暂未上传任何文档         │ ← 灰色提示文字
│  [+ 新建文档库]          │
└─────────────────────────┘
```

**点击「默认对话」：** 进入独立聊天页面，不关联任何产品文档库，可以正常对话、生成内容。适用于还没有整理好文档、或只想随意试用的场景。

**有文档库后：** 侧边栏结构如下：

```
┌──────────────────────────────────────────┐
│ 💬 默认对话                             │ ← 始终存在，不关联任何库
├──────────────────────────────────────────┤
│ 📁 阀门产品库                            │
│   💬 沙特阿美 Arabia                     │
│   💬 土耳其 Teknik                      │
│ 📁 泵类产品库                            │
│   💬 巴西 Nautilus                       │
├──────────────────────────────────────────┤
│ ── 尚未建档 ──（灰色虚线分隔）           │
│ 📨 询盘：伊朗石油·张工    2小时前       │ ← 灰色，无 customer_id
│ 📨 询盘：卡塔尔QP·王经理   昨天         │
│ 📋 新开发：迪拜Port·李总   3天前        │ ← 灰色，无 customer_id
├──────────────────────────────────────────┤
│ ── 正式客户 ──                           │
│ 📁 阀门产品库                            │
│   👤 沙特阿美 Arabia      3天前          │ ← 正常色，有 customer_id
│   👤 科威特 KPC           1周前          │
│ 📁 泵类产品库                            │
│   👤 沙特阿美 Arabia      3天前          │ ← 同一人，不同库
├──────────────────────────────────────────┤
│ [+ 新建文档库]         [+ 新客户]         │
└──────────────────────────────────────────┘
```

**视觉规则：**
- 「默认对话」：固定置顶，正常色文字，不可删除
- 未建档行（询盘/新开发）：灰色字，无边框
- 正式客户：正常色，有左侧竖线指示当前选中
- 三组之间：灰色虚线分隔
- 底部「+ 新建文档库」「+ 新客户」并列

**点击行为：**
- 点击「默认对话」→ 进入无库上下文，可聊天但无产品文档支持
- 点击未建档行 → 进入对应 conversation，prospect 上下文
- 点击正式客户 → 进入 customer 上下文（触发强制记忆层）
- 未建档对话转化后 → 该行自动消失，出现在对应文档库下

### 6.2 顶部：当前客户全局信息栏

当用户选中任意客户（无论来自哪个库），顶部显示该客户的全局档案：

```
┌─────────────────────────────────────────────────────┐
│ 👤 沙特阿美 Arabia          来源: 展会 | 类型: 进口商  │
│ 📅 最后接触: 3天前 | 待确认: X300报价 + 泵的MOQ      │
└─────────────────────────────────────────────────────┘
```

**侧边栏 ≠ 客户列表。** 侧边栏是「按库筛选客户的入口」，不是客户的唯一归属。

### 6.3 主界面

#### 默认对话的顶部栏

进入「默认对话」时，顶部固定栏显示：

```
┌─────────────────────────────────────────────────────────┐
│  💬 默认对话                              [🗑️ 清空]   │ ← 顶部固定栏（sticky）
│  无关联客户 · 无关联文档库                                │
└─────────────────────────────────────────────────────────┘
```

- 顶部左侧显示「💬 默认对话」
- 右侧 `[🗑️ 清空]` 按钮：清空当前对话历史，重新开始。**仅默认对话显示此按钮**，正式客户对话不提供清空按钮（保留完整会话记录）
- 副标题提示「无关联客户 · 无关联文档库」

```
┌─────────────────────────────────────────────────────────┐
│  👤 沙特阿美 Arabia — 阀门产品库          [🔒 锁定]     │ ← 顶部固定栏（sticky，不随滚动）
│  [写信✉️] [话术💬] [报价📊] [跟进📋]                      │ ← 始终可见
├─────────────────────────────────────────────────────────┤
│                                         │
│  对话内容区（可滚动）                    │
│  • 用户消息                              │
│  • AI 回复                              │
│  • 用户消息                              │
│  • AI 回复                              │
│  ...                                    │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │ 输入客户需求描述...               │   │
│  └─────────────────────────────────┘   │
│                          [生成开发信]    │
└─────────────────────────────────────────┘
```

**顶部固定栏（sticky）：** 客户信息 + 操作按钮始终固定在顶部，对话内容在下方滚动，选中未建档对话时显示对应阶段名称+来源。

**模式A — 对话模式：**
- 上部：历史消息流（用户+AI，一问一答）
- 下部：输入框 + [发送] / [生成话术] 等快捷按钮
- AI回复区显示引用来源文件名

**模式B — 生成功能（点击顶部按钮）：**
- [写信✉️] → 输入需求 → 生成开发信
- [话术💬] → 选择场景 → 生成话术
- [报价📊] → 输入型号配置 → 生成参考报价
- [跟进📋] → 显示待确认事项 → 生成跟进建议

**生成结果区（Ctrl+C/V 友好）：**
```
┌─────────────────────────────────────────┐
│ Dear Mr. Hassan,                        │
│ Thank you for your inquiry regarding...  │
│                                         │
│ Best regards,                           │
│ [Your Company]                          │
│                        [📋 复制] [🔄]   │
└─────────────────────────────────────────┘
```

- 生成内容为**纯文本**，无格式锁死，可直接 Ctrl+A → Ctrl+C 全选复制
- 右侧固定 [📋 复制] 按钮，点击复制全部到剪贴板 + 提示"已复制"
- [🔄 重新生成] [✏️ 编辑] 按钮并列
- **不做**：Word/PDF导出、在线富文本编辑（VibeRAG只是生成器，不是邮件草稿箱）

新建客户入口：侧边栏底部「+ 新建客户」按钮。

**核心原则：先查重，再建档。用户确认，同一客户不重复建档。**

#### 第一步：查重询问

用户输入客户名称后，系统**先查重，再建档**：

1. 用户输入客户名称
2. 系统模糊匹配 `customer` 表（LIKE %name%）
3. **命中** → 弹出确认框：
   > "这个名字在 [阀门库] 和 [泵库] 里出现过，是同一个人吗？"
   - 用户点「是的，关联」→ 关联已有 `customer_id` + 在当前库创建 `customer_library` 记录
   - 用户点「不是，是新人」→ 新建 `customer` + `customer_library` 记录
4. **没命中** → 直接进入新建表单

#### 第二步：新建表单（字段）

- 客户名称（必填，已填入）
- 来源（自动填入当前文档库名称，可修改）
- 客户类型（dropdown：进口商/出口商/中间商/终用户/其他）
- 联系人 / 邮箱 / 电话（选填）
- 备注（选填）

#### 第三步：跨库关联

表单底部「该客户同时属于」勾选框，列出当前 workspace 下所有文档库，勾选后自动创建 `customer_library` 记录。

#### 完整流程图

```
用户输入客户名称
    ↓
查重 → customer 表模糊匹配
    ↓
命中？→ 展示已有记录，问"是否同一人？"
    ├─ 是 → 关联已有 customer_id
    │       + 在当前库创建 customer_library
    └─ 否 → 新建 customer + customer_library

没命中 → 新建 customer + customer_library

最后 → 写入 customer.updated_at = now()
```

---

## 7. 安全模块

### 7.1 登录密码

**首次启用：设置密码**
- 首次启动显示引导页："请设置打开密码"
- 用户输入 2 次确认，密码加密存储（bcrypt）
- 无用户名，纯密码（单用户本地 app）

**每次启动：验证密码**
- 打开 app 显示密码输入框
- 正确 → 进入主界面
- 错误 → 提示"密码错误"，可重试

**修改密码**
- 路径：设置 → 安全 → 修改密码
- 输入当前密码（验证身份）→ 输入新密码 + 确认 → 成功更新
- 忘记密码：无服务器找回功能，参见§7.1.1

---

### 7.1.1 忘记密码处理

**数据分离设计：**
```
%APPDATA%/VibeRAG/
├── auth.db      ← 密码哈希，仅重置用
└── library.db   ← 客户档案+文档+向量，完全保留
```

**重置流程：**
1. 登录页点击"忘记密码？"
2. 提示用户手动删除 `auth.db` 文件："请在文件管理器中打开 %APPDATA%/VibeRAG/，删除 auth.db，然后重启 app"
3. 用户重启 app → 回到首次启动引导页 → 重新设置密码
4. `library.db` 完整保留，所有数据不丢失

---

### 7.2 自动锁屏

**锁定触发条件：**
- 打开 app 时（强制验证）
- N 分钟无鼠标/键盘操作（默认 3 分钟，可配置）
- 用户手动点击"锁定"图标（顶部信息栏右侧）

**手动锁定：** 顶部信息栏右侧放置锁定图标，用户离开工位前手动触发。顶部栏不随页面滚动移动，锁定图标始终可见。

**锁定状态：**
- 全黑遮罩（纯黑 #000000），主界面内容完全不可见
- 中央显示：VibeRAG 图标/文字 + 密码输入框
- 密码正确 → 解锁，回到锁定前界面
- 密码错误 → 提示"密码错误"，保持黑屏，不可跳过

**锁定期间：** 所有 AI 请求暂停处理，界面完全不可见。

---

## 8. 安装与部署

### 8.1 支持平台

| 平台 | 版本要求 | 安装包 |
|------|---------|--------|
| Windows | Windows 10+ (x64) | `.exe` 便携版，zip 压缩包 |
| macOS | macOS 12+ (Intel / Apple Silicon) | `.app` dmg 安装包 |

### 8.2 数据目录

| 平台 | 路径 |
|------|------|
| Windows | `%APPDATA%/VibeRAG/` |
| macOS | `~/Library/Application Support/VibeRAG/` |

```
VibeRAG/
├── auth.db          ← 密码哈希（可单独删除重置密码）
└── library.db       ← 客户档案+文档+向量
```

### 8.3 Ollama 依赖

- 用户需先安装 [Ollama](https://ollama.com/)（安装包很小）
- 系统启动时检测，未安装时弹出指引链接
- Embedding 模型：`nomic-embed-text`（首次启动自动拉取）
- 无需管理员权限即可安装和使用

### 8.5 对比 LLM Wiki 的改进计划

> 基于 `VibeRAG与LLMWiki文档处理对比分析.md`，梳理 VibeRAG 与成熟开源方案 LLM Wiki 之间的差距，明确后续改进方向。

#### P0 — v0.1 必须解决

**① 两步式 Ingest（LLM 先分析再分块）**
- 现状：暴力切分（500-800 tokens），不理解文档结构
- 目标：LLM 先分析文档结构 → 决定分块方式
  - 报价单 → 按表格结构分行/列分块
  - 产品手册 → 按章节/标题分块
  - 合同 → 按条款分块
  - 自由文本 → 按段落自然分块
- 实现：文档进来 → LLM 快速分析（返回结构类型+分块建议） → 按建议分块 → 入库

**② 持久化 ingest queue + 崩溃恢复**
- 现状：进程级队列，崩溃从头扫
- 目标：每扫描一个文件前写队列日志（FIFO），完成后标记
- 崩溃恢复：启动时读队列日志，跳过已完成文件

#### P1 — v0.2 应该做

**③ 目录结构作为分类 hint**
- 现状：纯扩展名+关键词判断文档类型
- 目标：路径如 `报价单/2024/Q1客户报价.pdf` → 解析为 `["报价单", "2024"]` 作为分类特征传入 LLM 分析阶段

**④ 扫描型 PDF 预检机制**
- 现状：直接标记 unsupported_ocr_required 或走 OCR
- 目标：先 try pdfplumber 文字提取 → 提取量 < 500 字 → 判定为扫描型 → 走 Tesseract OCR
- 原因：很多 PDF 是"文字型但编码混乱"，不是纯扫描，走 OCR 浪费

**⑤ 动态 overlap**
- 现状：固定 overlap 100 tokens
- 目标：按文档类型动态调整
  - 报价单：overlap 200-300（价格条款常跨段落）
  - 产品手册：overlap 100（章节边界清晰）
  - 合同：overlap 150（条款引用常跨段）

**⑥ 向量检索 + 图谱扩展重排**
- 现状：纯 top_k=5 向量相似度
- 目标：LLM Wiki 的 4-信号重排
  - 直接链接命中 × 3.0
  - 来源文件重叠 × 4.0
  - Adamic-Adar 关联 × 1.5
  - 类型亲和 × 1.0
- 实现：向量初筛 top 20 → 重排模型 → top 5 进生成

#### P2 — v1.0 锦上添花

**⑦ Source Traceability 精确到页码**
- 现状：chunk 只含 page_number，回答标注文件名
- 目标：引用时显示"来源：报价单.pdf 第3页"而非笼统的文件名

**⑧ auto-embedding 后台触发**
- 现状：同步入块后立即 embedding，用户看到等待
- 目标：新文件入库后后台触发 embedding，前端显示"向量生成中"

---

## 9. 开发计划

### v0.1 — 基础 RAG 闭环（最小可用版本）

**目标：** 扫描目录 → 解析文档 → 分块 → 向量化 → 检索 → 问答，最小闭环可运行。

**验收标准：**

1. 用户可指定本地目录，系统可成功扫描 TXT / MD / 文字型 PDF / DOCX
2. 相同文件重复扫描时不会重复入库（content_hash 命中则跳过）
3. 文件修改后 chunk / embedding 重新生成，旧数据清理
4. 文件删除后：
   - `file.deleted = 1`，chunk / embedding 物理删除
   - 检索结果不再返回已删除文件内容
5. 用户提问后，系统返回基于文档的回答
6. 回答中必须显示引用来源文件名
7. 如果检索相似度低于阈值（< 0.35），明确提示"未在文档中找到相关信息"，不编造答案
8. 扫描失败时记录错误原因，状态接口返回失败文件数
9. `/api/library/status` 返回：已扫描文件数 / 已处理文件数 / 失败文件数 / 状态（idle/scanning/indexing/failed/completed）
10. 扫描型 PDF / 图片文件：先 try 文字提取（< 500 字则判定为扫描型），自动走 Tesseract OCR，不报致命错误
11. **性能基准**：
    - 单个 10MB 以内 PDF 解析时间 < 30 秒
    - 向量检索响应时间 < 2 秒
    - 问答生成响应时间（本地模型）< 15 秒
    - 支持至少 1000 个文件同时索引

**功能列表：**

**P0（必须完成）：**
- [ ] 目录扫描 + 增量更新 + 删除检测
- [ ] **级联删除机制**：删除文件时同步清理 `file` → `chunk` → `embedding` 记录（保留 file 标记 `deleted=1`，chunk/embedding 物理删除）
- [ ] 支持格式：TXT / MD / PDF（文字型）/ DOCX / 图片（PNG/JPG/BMP）
- [ ] **两步式 Ingest**（LLM 先分析文档结构类型，决定分块方式）：
  - Step 1：LLM 快速分析文档结构（报价单表格/产品手册章节/合同条款/自由文本段落）
  - Step 2：按分析结果分块入库（不走固定 chunk size 暴力切分）
- [ ] **持久化 ingest queue**：每文件扫描前写队列日志，崩溃后恢复跳过已完成文件
- [ ] Ollama nomic-embed-text 向量化
- [ ] 向量存储（sqlite-vec）+ 检索
- [ ] RAG 问答：输入问题 → 检索 → 生成回复 → 显示来源

**P1（重要）：**
- [ ] 文档类型**规则判断**（扩展名+关键词，不调 LLM）：
  - 扩展名规则：`.pdf` → PDF / `.txt` → TXT / `.md` → MD / `.docx` → DOCX
  - 文件名关键词：报价单、quotation、price → 报价单；合同、contract → 成交记录；产品、手册、spec → 产品说明
  - 内容关键词：价格、单价、quantity → 报价单；产品参数、规格、型号 → 产品说明
  - 未匹配 → 默认类型：通用文档
- [ ] 文档类型**LLM 辅助分类**（目录路径作为 hint，如 `报价单/2024/` → 传入 LLM 分析阶段）
- [ ] 扫描型 PDF **预检机制**：先 try pdfplumber 提取文字，< 500 字再判定为扫描型 → 走 Tesseract OCR
- [ ] 基础错误处理（扫描失败、API 调用失败）
- [ ] 扫描状态查询接口（返回：已扫描文件数 / 已处理文件数 / 失败文件数）

**P2（增强）：**
- [ ] 动态 overlap（按文档类型：报价单 200-300 / 产品手册 100 / 合同 150）
- [ ] 进度显示 API（SSE 实时推送、预估剩余时间）→ *v1.0 完成 UI*

**不计入会话、不计客户、不计画像、不计生成功能。** 纯粹验证 RAG 链路可用。

---

### v0.2 — 客户与会话记忆

**目标：** 引入客户管理体系和会话记忆，RAG 问答与客户上下文绑定。

**验收标准：** 创建客户 → 上传文档关联该客户 → 对话自动带上该客户的文档上下文 → 关闭 app 后下次打开仍有记忆。

**功能列表：**

**P0（必须完成）：**
- [ ] 客户 CRUD + customer_library 关联
- [ ] customer_file 关联表（客户与具体文件关联）
- [ ] 入口拦截器（触发条件 + 降级策略，见下）
- [ ] 会话生命周期 API（start → turn → end）
- [ ] 每轮对话存入 conversation_turn（带时间戳）
- [ ] 会话摘要生成策略（触发时机，见下）

**P1（重要）：**
- [ ] 待确认闭环追踪（pending_items 跨会话持久化）

**P2（增强）：**
- [ ] XLSX / XLS 支持（基础文本提取，不做结构化分块）
- [ ] PPTX 支持（基础文本提取，不做幻灯片结构解析）

---

**入口拦截器详细设计：**

触发条件：
- 客户对话：每次请求前强制加载客户上下文
- **默认对话**：跳过客户上下文加载（无客户关联，纯 RAG 问答）
- 未建档对话（prospect/询盘阶段）：跳过客户上下文加载

上下文装配内容：
- 客户画像（customer 表）
- 会话摘要（session_summary）
- RAG 检索结果（当前问句的 Top-K 块）

缺失字段降级策略：
- 客户画像缺失 → **不阻断生成**，继续 RAG 问答 + 记录 warning 日志
- 会话摘要缺失 → 跳过，RAG 正常进行
- RAG 检索为空 → 返回"未找到相关内容"提示，不阻断生成

---

**会话摘要生成时机：**

触发方式三选一（满足任一即生成）：
1. 用户点击"结束会话"按钮时
2. 10 分钟无操作后自动触发
3. 累计 6 轮对话后生成中间摘要

生成内容：session_summary 写入 conversation_session 表，供下次会话的拦截器加载。

---

### v0.3 — 销售生成能力

**目标：** 基于客户档案和 RAG 上下文，生成销售专用内容。

**验收标准：** 选择客户 → 点击「生成开发信」→ 能生成一封包含客户信息和产品信息的内容 → AI 画像建议需要用户确认才写入。

**功能列表：**

**P0（必须完成）：**
- [ ] 4 个生成功能（开发信/话术/报价/跟进）
- [ ] LLM API 配置界面
- [ ] AI 画像建议流程（见下）+ 用户确认后才写入
- [ ] 引用来源展示（生成内容标注来自哪个文件）
- [ ] 报价建议质量控制：
  - 必须基于已识别报价单或人工输入价格，无价格来源时拒绝生成并提示"未找到价格信息"
  - 显示价格来源文件名，标记"待人工确认"
  - 报价单识别规则：文件名含"报价"/"price"/"quotation" 或 内容含"单价"/"price per unit"

**P1（重要）：**
- [ ] PDF（图片扫描型）OCR 支持
- [ ] PNG / JPG / BMP 图片文字 OCR 支持
- [ ] HTML 格式支持

**P2（增强）：**
- [ ] XLSX 结构化分块（按 Sheet/表格结构解析）

---

**AI 画像建议实现流程：**

1. 会话结束后，AI 分析 conversation_turn 历史记录
2. 提取候选画像字段（偏好产品/预算区间/主要竞争对手/决策人性格等）
3. 写入 `pending_profile_updates` 表（待确认状态）
4. 用户在客户详情页查看 AI 建议列表，逐条确认：
   - 接受 → 写入 `customer` 表对应字段
   - 拒绝 → 标记为已拒绝，不重复建议
   - 忽略 → 下次会话仍可再建议
5. 高风险字段（预算/竞争对手/决策人性格）**必须用户显式确认**，不做静默写入

---

**隐私模式配置（v0.3 增加）：**

| 模式 | Embedding | LLM 生成 | 文档分类 |
|------|-----------|---------|---------|
| 本地模式（默认）| Ollama 本地 | API Key（内容上传第三方）| 规则判断 |
| 云端增强模式 | Ollama 本地 | API Key（内容上传第三方）| LLM 判断（需用户确认）|

- 模式切换入口：设置 → 隐私模式
- 切换时弹出明确提示：告知哪些数据会上传
- 本地模式：文档不离开本地（Embedding + 规则分类均在本地）
- 云端增强模式：明确告知文件片段会上传到 LLM 服务商

---

### v1.0 — 产品化

**目标：** 完整可用、可安装、可交付。

**功能列表：**

**P0（必须完成）：**
- [ ] 完整错误处理 + 用户友好错误提示
- [ ] Windows 安装包（.exe）
- [ ] macOS 安装包（.app）
- [ ] 演示数据（Sample 库）

**P1（重要）：**
- [ ] 扫描进度显示优化（SSE 实时推送 + 进度条 + 预估剩余时间 + 当前处理文件名实时显示）
- [ ] 数据迁移工具：
  - 自动检测数据库版本，支持旧版本升级
  - 备份旧数据后再迁移
  - 迁移失败自动回滚机制
  - 迁移进度可视化

---

## 10. 技术风险与缓解

| 风险 | 缓解 |
|------|------|
| Ollama 未安装 | 启动检测，未安装时弹出指引链接 |
| API Key 无效 | 调用前验证，错误提示 |
| 大目录扫描慢 | 后台异步扫描 + 进度推送（Server-Sent Events） |
| 中文 OCR 质量差 | Tesseract 4.0+ chi_sim 训练数据 |
| 表格数据分块破碎 | 按表格结构分块，不破坏行列关系 |
| sqlite-vec 稳定性 | v0.1.5+ 经过生产验证 |

---

## 11. 备注

- **完全本地运行，文档不离开客户电脑** — 但需要注意：

- Embedding（向量化）：调用本地 Ollama，完全本地
- LLM 生成：使用客户自备的 API Key（OpenAI/Claude 等），内容会上传到第三方 LLM 服务商
- **文档分类（v0.1）**：优先使用规则判断（扩展名+关键词），避免上传内容到云端
- **文档分类（云端模式，可选）**：如需 AI 分类，用户手动选择"云端增强模式"，明确告知会上传文件片段
